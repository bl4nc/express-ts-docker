# Build a express TS application Templete for docker

---

## Guia

### Build express App
Run commands.
`npm i`
`npm run build`

### Build a docker container
Run commands.
`docker-compose up -d --build`
